# Activity — 2026-07-12

- **Codex/OpenAI passthrough tests + E2E harness landed on `feat/codex-e2e-support`** — Verified the existing transparent capture proxy works for Codex CLI traffic with no proxy code changes: 2 new vitest cases (`src/proxy.test.ts` +220 lines) cover `POST /v1/chat/completions` and `POST /v1/responses` with path-verbatim passthrough, Bearer passthrough, on-disk auth redaction, body round-trip, capture file write. New `scripts/test-codex-e2e.mjs` (~290 lines) spins up a mock OpenAI upstream + runs sandboxed codex (`CODEX_HOME=/tmp/codex-test-e2e`, fake auth, custom `openai-custom` provider pinned to the proxy) to capture real Codex CLI traffic end-to-end. README gained a "Using with Codex" section documenting the `config.toml` recipe (codex does NOT honor `OPENAI_BASE_URL` for the built-in `openai` provider — must use a custom provider name). 130/130 unit tests pass, E2E captures 6 `/v1/responses` requests with 66KB bodies each. `~/.codex/auth.json` untouched (mod time Jul 9). Branch not pushed per user choice (option 1).

## Next steps

- User to push `feat/codex-e2e-support` + open PR when ready (`gh pr create --base main --head feat/codex-e2e-support`).
- Out-of-scope: the `proxy.test.ts` Fibonacci describe-block has a pre-existing `afterAll` cleanup race (ENOTEMPTY on `.test-captures/` because `saveCapture` is fire-and-forget). Reproduces on `main` without our changes; separate fix.
- Out-of-scope: `llm-inspector` repo still has no GitHub Actions workflow (memory `feedback_2026-06-24_llm-inspector-no-ci.md`). Adding `.github/workflows/test.yml` is the natural follow-up so this test suite runs on every PR.