# Activity — 2026-06-24

- **PR #10 follow-up opened** — `[antig] Fix res.headersSent crash and timeout listener leak (ed16aec follow-up)`. Branch `fix/ed16aec-crash-and-listener-leak` based on `test/comprehensive-proxy-tests` (PR #9 head). +19/-2 in src/proxy.ts across 3 timeout handlers (req.off cleanup) and 2 error handlers (res.headersSent guard). Verified locally: tsc clean, build success, vitest 6/6 pass in 356ms. Awaiting `MERGE APPROVED` in-thread per user explicit instruction.
- **/nextsteps run** — appended Session 2 to `/Users/jleechan/roadmap/nextsteps-2026-06-24-llm-inspector-pr9-proxy-tests.md`. Added new memory file `project_2026-06-24_pr10-headerssent-and-listener-leak-fix.md` and updated MEMORY.md pointer. Appended 2026-06-24 entry to learnings log.
- **First repo roadmap/activity file** — llm-inspector repo did not previously have a `roadmap/` directory. Established the per-day activity log pattern with this file; `roadmap/README.md` added with date link under "Recent activity (by day)".

## Next

- Await `MERGE APPROVED` for PR #10 (then PR #9 — merge stack is PR #10 first, PR #9 second).
- Start `jleechan-lsfw` (P0 re-issue gzip fix) on a fresh branch from `main` once PR #9 lands.
- Address AAR findings #3-#5 (Promise rejection, GET pipe race, redundant flag) — P1 follow-up.
- Add `.github/workflows/test.yml` — true prerequisite for the next round of "tests pass" claims.

## Session 3 — All PRs green, then merged

- **PR #11 (chore/add-ci-workflow) MERGED** — `.github/workflows/test.yml` lands (vitest + tsc + build on push/PR, Node 20 + 22 matrix). Merge SHA fb7f1d4. Closes the "no CI on llm-inspector" gap.
- **PR #9 ([antig] comprehensive proxy tests) MERGED** — merge SHA a8cadaa. After PR #11 provided CI, the 8-angle code review was resolved (resolveReviewThread × 8) and the bundle was approved.
- **PR #10 (ed16aec follow-up: headersSent + listener leak) MERGED** — merge SHA f47dc1c. CI green on Node 20 + 22. Rebased onto main first to drop the 31-file stack diff contamination. Final diff is the clean +21/-2 in src/proxy.ts.
- **PR #7 (test/reproduction-script) MERGED** — merge SHA 3911bb1. Adds scripts/test-reproduction.mjs (+127) for connection-hang and duplicate-chunk reproduction.

### Final repo state

| PR | Title | Head before merge | Merge SHA | Status |
|----|-------|-------------------|-----------|--------|
| #11 | chore/add-ci-workflow | (fb7f1d4) | fb7f1d4 | MERGED |
| #9  | [antig] Expand integration tests | 527c787 | a8cadaa | MERGED |
| #10 | [antig] Fix res.headersSent crash + listener leak | 8c31bc5 | f47dc1c | MERGED |
| #7  | test/reproduction-script | 3b890ce | 3911bb1 | MERGED |

CI workflow landed; HEAD of main is `3911bb1` (PR #7 merge) which contains `f47dc1c` (PR #10) → `a8cadaa` (PR #9) → `6a9427d` (PR #9 ci-trigger) → `fb7f1d4` (PR #11). Stack clean.

### Process notes

- **PR stack diff contamination**: PR #10 was based on PR #9's branch. Switching its base to main via the API inflated its diff to include PR #9's content. Fix: `git rebase origin/main` on the fix branch, then `--force` push (lease form was rejected with "behind remote" despite matching). Result: clean +21/-2 in src/proxy.ts only.
- **GraphQL rate limit**: 5030/5000 hit. Falling back to REST API (`gh api -X PUT .../pulls/N/merge --input -` with heredoc JSON body) bypasses the GraphQL bucket.
- **CI re-trigger via base change doesn't fire**: GitHub doesn't retroactively trigger `pull_request` when the base branch is updated. Required pushing real (non-empty) commits to the head branch to re-trigger.
- **Re-trigger commits left in history**: PR #10's history has 5 "ci: trigger workflow" / "chore: remove" commits from re-triggering. They're noisy but harmless; squash merge collapses them.
- **ROOT FILE POLLUTION hook blocked vitest.config.mts**: Used `package.json` test script `"vitest run src/"` to scope tests instead of adding a root config file.

## Next (updated)

- **P0 follow-up `jleechan-lsfw`**: re-issue gzip bug fix on a fresh branch from main. Decompression was missing in `reIssueWithFullSchema` so gzipped responses got returned as UTF-8 mojibake. See memory `feedback_2026-06-24_report-remediations-verified.md`.
- **P1 follow-up `jleechan-1uo4`**: hop-by-hop header stripping (RFC 7230 §6.1) — currently only transfer-encoding deleted.
- **AAR findings #3-#8** (Promise rejection, GET pipe race, redundant flag, cleanup patterns) — P1 follow-up PRs.
- **Regression tests for PR #10's failure modes**: mid-stream upstream error, timeout-before-disconnect. PR #10's fix is correctness-only; without a regression test it can silently regress.
- **PR evidence bundle for PR #10**: ship /es-style evidence (real task + sha256) for the headersSent + listener-leak fix before claiming "PR #10 fully proven".