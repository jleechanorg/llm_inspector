# Activity — 2026-06-25

- **PR #12 MERGED** — `[antig] fix(proxy): decompress gzip on reIssueWithFullSchema response`. Two fixes in one PR: (1) re-issue gzip decompression at `src/proxy.ts:234-271` — detects content-encoding: gzip, pipes through createGunzip before buffering; (2) PR #10 mid-stream regression test at `src/proxy.test.ts` — covers the `if (!res.headersSent)` guards at proxy.ts:756 and proxy.ts:787. Merge SHA: `3abf57cc1d2e9660c46d75807bf6359b35b4fecd`. Closes bead `jleechan-lsfw` (re-issue gzip fix, P0). CI green on Node 20 + 22.
- **SHA-256 replay oracle test added** — closes /es C6 (HIGH severity) gap from the PR #9 review. New test `preserves request bytes byte-for-byte: client = upstream = capture (SHA-256 replay oracle)` asserts `sha256(client_sent) === sha256(upstream_received) === sha256(capture_file.request_raw)`. Frozen payload SHA-256: `92bc7bd1726eefb2a3bbcfb22e76e95660cde87d17a5bfdd2976678865f78a6e`. Direct-to-main commit `0b4c82b` (not via PR — the SHA-256 oracle is a test addition, no production code change).
- **Evidence bundle shipped** — `docs/evidence/pr12-gzip-re-issue-fix-2026-06-25/` conforms to /es canonical shape: Claim → Artifact Map with `[Layer N source]` labels, "What This Evidence Does NOT Prove" section, canonical metadata.json fields, run.json scenarios[] with campaign_id, sha256 sidecars in `sha256sum -c` compatible format (all 14 files verify clean). Bundle includes vitest verbose log + frozen oracle payload + build artifacts (dist/cli.js, dist/cli.d.ts).
- **Test count**: 125 vitest tests pass (was 124 pre-PR-#12; +1 oracle). Side-by-side: PASS — 86.2% payload reduction, 86.5% tool def reduction, 0 duplicate chunks, re-issue success.
- **Discovered PR #8 was a no-op for on-demand gzip** — `fix/compression-zlib-round2` (commit 41838ff) was closed without merge, but the nextsteps doc said it was the canonical fix for the re-issue gzip bug. Reading the actual diff showed 41838ff only modified `startProxy`'s response handler — the response-handler gzip fix had already landed in main via another path. The on-demand mojibake bug was a separate, fresh fix that PR #12 had to ship. Memory recorded in `feedback_2026-06-25_pr8-missed-on-demand-gzip.md`.

## Next

- **Layer 3 (real-LLM) re-verification** — the prior real-LLM bundle (`compression-zlib-repro-2026-06-24`) was sealed before PR #12's fix landed. A new bundle should re-run `claude --print <prompt>` through the post-fix proxy and assert capture-file round-trip equality. Deferred until next session.
- **PR #10 listener-leak fix coverage** — the `req.off("close", onClientClose)` cleanup at the three timeout handlers (proxy.ts:251, 474, 955) is not covered by a regression test. Hardcoded 120s timeout makes direct testing impractical without injecting a `timeoutMs` parameter to `startProxy`.
- **Hop-by-hop headers fix (RFC 7230 §6.1)** — bead `jleechan-1uo4` (P1). Currently only `transfer-encoding` deleted; `Connection`, `Keep-Alive`, `Proxy-Authenticate`, `Proxy-Authorization`, `TE`, `Trailer`, `Upgrade` still forwarded.
- **analyzer.ts / cli.ts / utils.ts test coverage** — 0% on these files (391 + 321 + 263 LOC). Pre-PR-#11 had no CI gate either. PR #11 gave them a CI gate but no tests exist.
